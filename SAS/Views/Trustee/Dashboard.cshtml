@using SAS.ViewModels
@{
    ViewData["Title"] = "Trustee Dashboard";



    var profile = ViewBag.Profile as UserViewModel;

    var teachers = ViewBag.Teachers as List<DetailsViewModel>;

    var staffs = ViewBag.Staffs as List<DetailsViewModel>;

    var principals = ViewBag.Principals as List<DetailsViewModel>;
}

<div class="container mt-4">
    <!-- Profile -->
    <div class="card mb-4">
        <div class="card-body">
            <h3 class="text-primary">
                Welcome, @profile?.Name
                <small class="text-muted">(@profile?.Role)</small>
            </h3>
            <p><strong>Email:</strong> @profile?.Email</p>
        </div>
    </div>

    <vc:notice></vc:notice>
    @await Component.InvokeAsync("ReportBug", new { name = profile?.Name, email = profile?.Email })

    <!-- Principals Section -->
    <div class="card mt-4">
        <div class="card-header bg-dark text-white">Principals</div>
        <div class="card-body">
            @if (principals != null && principals.Any())
            {
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>Photo</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Phone</th>
                            <th>Address</th>
                            <th>Salary</th>
                            <th>DOB</th>
                            <th>Experience</th>
                            <th>Joining Date</th>
                            <th>Subjects</th>
                            <th>Standards</th>
                            <th>Qualifications</th>
                            <th>Documents</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var principal in principals)

                        {
                            <tr>
                                <td>
                                    @if (!string.IsNullOrEmpty(principal.details?.Photo))

                                    {
                                        <img src="@principal.details.Photo" alt="Photo" style="width:50px;height:50px;" />
                                    }
                                </td>
                                <td>@principal.user?.Name</td>
                                <td>@principal.user?.Email</td>
                                <td>@principal.details?.Phone</td>
                                <td>@principal.details?.Address</td>
                                <td>@principal.details?.Salary?.ToString("C")</td>
                                <td>@principal.details?.Dob.ToShortDateString()</td>
                                <td>@principal.details?.Experience</td>
                                <td>@principal.details?.JoiningDate.ToShortDateString()</td>
                                <td>@string.Join(", ", principal.details?.Subjects ?? new List<string>())</td>
                                <td>@string.Join(", ", principal.details?.Std ?? new List<int>())</td>
                                <td>@string.Join(", ", principal.details?.Qualifications ?? new List<string>())</td>
                                <td>
                                    @if (principal.details?.Documents != null)

                                    {
                                        @foreach (var doc in principal.details.Documents)

                                        {
                                            <a href="@doc" target="_blank">View</a>

                                            <br />
                                        }
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p class="text-muted">No principals found.</p>
            }
        </div>
    </div>

    <!-- Teachers Section -->
    <div class="card mt-4">
        <div class="card-header bg-primary text-white">Teachers</div>
        <div class="card-body">
            @if (teachers != null && teachers.Any())
            {
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>Photo</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Phone</th>
                            <th>Address</th>
                            <th>Salary</th>
                            <th>DOB</th>
                            <th>Experience</th>
                            <th>Joining Date</th>
                            <th>Subjects</th>
                            <th>Standards</th>
                            <th>Qualifications</th>
                            <th>Documents</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var teacher in teachers)

                        {
                            <tr>
                                <td>
                                    @if (!string.IsNullOrEmpty(teacher.details?.Photo))

                                    {
                                        <img src="@teacher.details.Photo" alt="Photo" style="width:50px;height:50px;" />
                                    }
                                </td>
                                <td>@teacher.user?.Name</td>
                                <td>@teacher.user?.Email</td>
                                <td>@teacher.details?.Phone</td>
                                <td>@teacher.details?.Address</td>
                                <td>@teacher.details?.Salary?.ToString("C")</td>
                                <td>@teacher.details?.Dob.ToShortDateString()</td>
                                <td>@teacher.details?.Experience</td>
                                <td>@teacher.details?.JoiningDate.ToShortDateString()</td>
                                <td>@string.Join(", ", teacher.details?.Subjects ?? new List<string>())</td>
                                <td>@string.Join(", ", teacher.details?.Std ?? new List<int>())</td>
                                <td>@string.Join(", ", teacher.details?.Qualifications ?? new List<string>())</td>
                                <td>
                                    @if (teacher.details?.Documents != null)

                                    {
                                        @foreach (var doc in teacher.details.Documents)

                                        {
                                            <a href="@doc" target="_blank">View</a>

                                            <br />
                                        }
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p class="text-muted">No teachers found.</p>
            }
        </div>
    </div>

    <!-- Staffs Section -->
    <div class="card mt-4">
        <div class="card-header bg-secondary text-white">Staffs</div>
        <div class="card-body">
            @if (staffs != null && staffs.Any())
            {
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>Photo</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Salary</th>
                            <th>DOB</th>
                            <th>Experience</th>
                            <th>Joining Date</th>
                            <th>Phone</th>
                            <th>Address</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var staff in staffs)

                        {
                            <tr>
                                <td>
                                    @if (!string.IsNullOrEmpty(staff.details?.Photo))

                                    {
                                        <img src="@staff.details.Photo" alt="Photo" style="width:50px;height:50px;" />
                                    }
                                </td>
                                <td>@staff.user?.Name</td>
                                <td>@staff.user?.Email</td>
                                <td>@staff.details?.Salary?.ToString("C")</td>
                                <td>@staff.details?.Dob.ToShortDateString()</td>
                                <td>@staff.details?.Experience</td>
                                <td>@staff.details?.JoiningDate.ToShortDateString()</td>
                                <td>@staff.details?.Phone</td>
                                <td>@staff.details?.Address</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p class="text-muted">No staffs found.</p>
            }
        </div>
    </div>
</div>